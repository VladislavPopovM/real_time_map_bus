# 🚌 Онлайн-карта движения автобусов 📍

<video src="https://github.com/user-attachments/assets/fe033e74-15e2-4658-99d9-b7b3bac399b6" width="100%" autoplay loop muted playsinline></video>

Высокопроизводительная система мониторинга городского транспорта в реальном времени. Проект объединяет ультра-быстрый асинхронный бэкенд на **Trio**, бинарный протокол передачи данных и реактивный фронтенд в стиле "Cyberpunk" на **Svelte 5**. 🚀

---

## 📂 Файловая структура проекта

```text
.
├── server/                  # ⚙️ High-Performance Backend (Python + Trio)
│   ├── routes/              # 🛣️ База маршрутов Москвы (595 JSON-файлов)
│   ├── server.py            # 🖥️ Главный сервер (Бинарный протокол, Spatial Index)
│   ├── fake_bus.py          # 🤖 Имитатор (Trio Channels, мультиплексирование)
│   ├── harmful_client.py    # 🛡️ Тест безопасности: Браузер
│   └── harmful_bus.py       # 🛡️ Тест безопасности: Автобус
│
├── frontend/                # 🎨 Reactive Frontend (Svelte 5 + Leaflet)
│   ├── src/lib/
│   │   ├── Map.svelte       # 🖌️ Рендерер (Game Loop, Canvas, Интерполяция)
│   │   └── busStore.svelte.js # 📦 Бинарный парсер и хранилище данных
│   └── index.html           # 🏠 Точка входа
│
└── README.md                # 📖 Главный документ
```

---

## 🧠 Техническая «начинка» (Тонкости) ✨

### 1. Алгоритмы и Математика 🧠
*   **LERP + EaseOut**: Автобусы двигаются с частотой **60 FPS** благодаря интерполяции с функцией плавного затухания.
*   **Batch Rendering**: Отрисовка **20,000+ объектов** через Canvas API с группировкой по цветам.
*   **Синхронизированный Zoom**: Использование `overlayPane` и координат слоя для идеальной привязки точек к карте при масштабировании.

### 2. Оптимизация и Протоколы 🛠️
*   **Бинарный протокол (Binary-over-WS)**: Передача данных через `Float32Array`. Пакет на 20к объектов весит всего **320 Кб**.
*   **Spatial Indexing**: Сервер фильтрует данные на основе границ видимости пользователя, используя высокоскоростную сетку.
*   **Trio Channels**: Мультиплексирование тысяч потоков данных в 5-10 стабильных WebSocket-соединений.

---

## 🛠️ Установка и Запуск

### 1. Бэкенд 🐍
```bash
pip install -r server/requirements.txt
python server/server.py -v
```

### 2. Имитатор 🤖
```bash
# Запуск 20,000 автобусов через 10 сокетов
python server/fake_bus.py --buses_per_route 34 --websockets_number 10
```

### 3. Фронтенд 🎨
```bash
cd frontend
yarn install
yarn dev
```
👉 Откройте: **`http://localhost:5173`**

---

## 📊 Телеметрия (HUD)
В правом верхнем углу отображается:
*   **NEXUS ACTIVE**: Статус соединения.
*   **UPS (Updates Per Second)**: Частота входящих пакетов.
*   **TRACKED UNITS**: Живой счетчик объектов на экране.

---
**Status:** `MISSION ACCOMPLISHED` | **Tech Stack:** `Python Trio` & `Svelte 5` 🚌✨🛰️
